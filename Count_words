//counting the characters and then return it


#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    char s[20];
    int count=0,i;
    cin.getline(s,200);
    for(i=0;s[i]!='\0';i++)
    {
        if (s[i]==' ')
        count++;
    }
    cout<<count+1;
    return 0;
}
